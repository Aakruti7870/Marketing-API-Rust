name: Deploy to AWS EC2 Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Validate workflow
        run: echo "DEPLOY_WORKFLOW_VALID"
